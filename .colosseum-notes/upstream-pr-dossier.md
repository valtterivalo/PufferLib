# Upstream PR dossier (2026-07-16, HEAD post-cleanup)

State of our 5.0 vs upstream for the next OSRS PR. Produced by the
osrs-upstream-prep-analysis workflow (48 agents: hunk mappers vs upstream/5.0
AND upstream/5c, hygiene audit, adversarial verification, completeness critic),
synthesized and spot-verified by the orchestrator.

## Branch geometry

- upstream/5.0 is FROZEN: its tip c3e28db18 is our merge base, we are +353
  commits, they are +0. There is nothing to rebase onto on that branch.
- upstream/5c is the ACTIVE line (+66 over the 5.0 base at analysis time,
  including their own pufferl cleanup and a src reshuffle: puffernet.h ->
  puffercpu.h, pufferlib.cu -> algo.cu/pufferl.cu). The PR must cut FROM 5c.
- Trial merge 5c -> our 5.0: 40 conflicts, dominated by config/*.ini churn.
  Decision: do NOT absorb 5c into trunk. Build the PR branch from upstream/5c
  and port the curated payload onto it.

## Internals diff classification (vs upstream/5.0, cross-checked against 5c)

UPSTREAMABLE FIXES (candidate standalone PRs, or lead commits of the env PR):
- Truncation channel end-to-end (vecenv.h + pufferlib.cu) + truncation-aware
  GAE (nextboundary stops the lambda bootstrap through timeouts). 5c still
  lacks it. The single highest-value general fix we carry.
- ppo_clamp_logratio [-10,10] before __expf: many-head joint log-prob overflow
  guard. Identity for |logratio| <= 10. Absent in 5c (algo.cu:1708).
- create-time assert num_action_heads <= MAX_ATN_HEADS (5c has the constant
  but not the guard).
- bindings_cpu.cpp / bindings.cu: 3 fixes each classified upstreamable (parse
  guards; see workflow journal for hunks).
- torch_pufferl.py: 6 upstreamable hunks (torch-lane parity fixes).
- pufferl.py: sweep objective metrics[target_key] fix (upstream hardcodes
  env/score); resume_from_log_dir observation replay is LOCAL-ONLY flavored
  but generally useful — offer, do not insist.

ENV COUPLING (ships only as part of the OSRS env PR, needs 5c porting):
- ColosseumEntityEncoder: fused CUDA train path (ocean.cu ~525 lines),
  inference mirror (puffernet.h -> 5c puffercpu.h), hypers.entity_encoder
  plumbing, models.py torch reference (test oracle only, documented as such).
- LOG_DICT_CAPACITY 96 (vecenv.h) + log_environments_impl capacity constant.
- Best-trajectory curriculum rewrite (pufferlib.cu + curriculum.cu +
  vecenv.h static_vec_reset state-restore + post_step_callback). BIG upstream
  ask; 5c has none of it. Either propose as its own feature PR or gate the
  env's late-start mode to degrade without it. curriculum.cu has 5 hunks the
  mapper marked unclear — needs a human pass before any PR.
- build.sh: osrs asset-manifest generation step + env registration.

ALREADY IN 5c (drop from any PR): select_copy row_importance removal,
per-bank buffer_states reset zeroing.

## PR blockers found by the completeness critic

1. FIXED: root *.bin checkpoints unignored (now /*.bin in .gitignore).
2. asset_manifest.json fetches from valtterivalo fork releases (v22 tarball).
   PR needs upstream-hosted assets or a documented fetch override.
3. FIXED: osrs_inferno.ini carried six orphaned curriculum knobs deleted from
   HypersT. NOTE: upstream boxoban.ini/g2048.ini also carry orphaned
   state_buffer_size/explore_* keys + sweep sections IF the curriculum rewrite
   ships — must be fixed in the same PR that ships the rename, else dropped.
4. FIXED: COLOSSEUM_SOTA.md deleted (stale, misdirecting, W&B leaks).
5. export_colosseum_items.py hardcoded /Users/valtterivalo default path
   (fixed in cleanup); osrs_colosseum.ini:248 comment leaks W&B entity —
   scrub during PR curation, the ini header gets rewritten then anyway.
6. osrs_combat_visuals_generated.h cites a generator that no longer exists
   (fixed in cleanup: honest hand-maintained note like items_generated).
7. FIXED (documented): torch ColosseumEntityEncoder is the finite-diff test
   reference, not a live eval path.
8. tools/cache_pipeline ships a full Jagex cache decoder (incl. XTEA) —
   copyright optics DECISION NEEDED before PR: strip from payload vs ship.
   Raw cache bytes are gitignored; the decoder code itself is ours.
9. osrs_assets_generated.h is tracked AND regenerated by build.sh every osrs
   build — accept manifest-driven churn (deterministic given manifest) or
   untrack. Leaning: keep tracked, regeneration is deterministic.
10. ocean/osrs_colosseum/cpu_stubs/cuda_runtime_api.h shim enables --cpu
    builds; upstream will scrutinize — flag in the PR description.

## Local git housekeeping (done 2026-07-16)

- 14 merged topic branches deleted; dead workflow worktrees pruned.
- 5.0-clexp + 5.0-simplecl archived to origin (were local-only).
- Kept for later: fix-vtrace-rho-scaling (1-commit trainer fix, never
  adopted — evaluate against 5c before PR), colo-prayer-oracle (likely
  superseded by the 07-10 oracle fix), colo-obs-equipcut + osrs-asset-pipeline
  (dead experiments, pushed), pvp-* x3 (dormant campaign, ~110 commits each).
- 8 stashes from Mar-Jun on dead branches: recommend dropping all (list in
  session log); none touched.

## Hygiene cleanup (executed via osrs-cleanup-execution workflow)

124 adversarially-verified findings; 111 applied across 7 clusters
(colosseum/inferno/zulrah/pvp/shared/rendergui/tests+tools), 13 structural
items DEFERRED to a post-PR pass:
- Log struct: move encounter-prefixed accumulators out of osrs_types.h into
  per-encounter logs (ABI-wide).
- osrs_special_attacks.h: derive roll-mode + force-max interpreters from one
  per-weapon descriptor table.
- molten pools: 5 parallel arrays -> ColoMoltenPool struct array.
- pvp drain_type/magic_type enums (dormant campaign, do with pvp revival).
- test_inferno_attack_styles.c: 10k lines, split + replace 73 source-grep
  asserts with behavioral checks.
- obs_mask.inc + other oversize splits.

Regression gates for every applied batch: colosseum battery + goldens +
spear parity, inferno battery + goldens, shared osrs test suite, pvp tests,
--cpu and --local builds on trunk after merge.

## 2026-07-16 PR-readiness verdicts (user review + fable verification)

STRIP FROM PR (keep local): .colosseum-notes/, tests README, root tests/*.py + probe,
tools/osrs-link-data.sh, sweep-resume + anchor-fix (pufferl.py) + sweep.py -> separate PRs.
REVERT ON TRUNK (inert, verified): trainer best-trajectory curriculum cluster
(curriculum.cu rewrite, vecenv reset-with-state + post_step_callback, pufferl.cu hooks,
binding PUFFER_RESET_WITH_STATE shims) - cl_frac/num_start_states/fresh_frac = 0 in every
shipped config; late_start_state_mode is env-side and independent. LOG_DICT_CAPACITY 96
bump (colosseum 23 keys, inferno 33 - both fit 64).
SEPARATE MINI-PRS (load-bearing, general): truncation channel + truncation-aware GAE +
torch mirror (landmark timeout-collapse fix); ppo_clamp_logratio + MAX_ATN_HEADS raise
+ assert (20-head NaN guard).
OSRS PR PROPER: env payload + build.sh asset fetch (fork-hosted per Suarez agreement,
must just-work on fresh clone) + encoder TBD.
ENCODER: upstream 5c has MinimalEntityEncoder (ME_*) - generalization conversation with
Suarez, not a colosseum-named core struct. Fable clone drafting proposal options.
RETRAIN GATE BEFORE PR: inferno/zulrah/pvp configs must demonstrably learn on box.

## 2026-07-17 adversarial review verdict (full report: scratchpad internals_adversarial_review.md, copied below-worthy items)
BLOCKERS before mini-PR carving: (1) 5c deleted kernels.cu/bindings*.cu(cpp)/
vecenv.h/models.cu AND the entire pybind layer -- both mini-PRs are PORTS to the
new algo.cu/pufferl.cu/pufferenv.h/puffercpu.h seam, not cherry-picks; hold until
the Suarez conversation fixes a target. (2) vec_log 32->64 re-bucketed DROP-FROM-PR
(5c Dict auto-grows, bug does not exist there). (3) MAX_ATN_HEADS: 5c carries
Suarez's own TODO "use env atn dim directly" -- implement that TODO or arrive with
measurements; a bare bump contradicts him. Scrubs applied 92c082a1a incl. the
REAL latent sweep bug (result_queue reported hardcoded env/score, not
metrics[target_key] -- memory overstated the old fix). LOW open: boot assert ->
fprintf+abort at PR time; truncation PR needs an upstream in-tree consumer story.
